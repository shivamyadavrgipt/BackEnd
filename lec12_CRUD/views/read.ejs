<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full h-full bg-zinc-900">
    <div class="w-full h-full bg-zinc-900 text-white p-10">
        <a class="text-blue-500" href="/">Back Home</a>
        <h1 class="text-3xl mt-10 tracking-tighter mb-3">ALL USERS</h1>
        <div class="users flex flex-wrap gap-3">
            <%users.forEach(function(user){%>
                <div class="user w-72  bg-zinc-800 rounded-lg outline-none mt-7">
                <div class="w-full h-72 rounded-lg bg-zinc-700 overflow-hidden">
                    <img class="w-full h-full user object-top" src="<%=user.image%>" alt="">
                </div>
                <h3 class="text-xl mt-3 tracking-tighter pl-3"><%= user.name%></h3>
                <h5 class="text-zinc-500 pl-3"><%=user.email%></h5>
                <div class="flex gap-3 mt-10 justify-evenly pb-4">
                    <a  href="/edit/<%=user._id%>">Edit</a>
                    <a class="text-red-300" href="/delete/<%=user._id%>" >Delete User</a>
                </div>
        </div>
        <%})%>
    </div>
    </div>
</body>
</html>
