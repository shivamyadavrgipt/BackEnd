<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylecss/style.css">
    <script src="https://cdn.tailwindcss.com"></script> 
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input placeholder="Input text.." type="text" class="block outline-none w-full rounded-md py-2 bg-zinc-800 resize-none " name="title">
                <textarea placeholder="write your text here.." class="block outline-none mt-2 w-full rounded-md py-5 bg-zinc-800 resize-none" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length>0){%>
            <% files.forEach(function(val){%>
                <div class="task w-72 px3 py-4 bg-zinc-800 p-2 ">
                    <h1 class="text-white text-3xl tracking-tighter"><%= val%></h1>
                    <div class="flex justify-between w-full items-centre p-1 mt-4">
                        <a class="text-blue-500 inline-block" href="/files/<%=val%>">Read more</a>
                        <a href="/edit/<%= val%>" class="text-zinc-600">Edit</a>    
                    </div>
                </div>
            <%})%>
            <%} else{%>
                <h3 class="text-zinc-500">No Task yet...</h3>
            <%}%> 
    </div>
    </div>
    <script src="/javascript/script.js"></script>
</body>
</html>